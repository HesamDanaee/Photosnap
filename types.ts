interface HeroTextContent {
  title: string;
  text: string;
  ctatext?: string;
  bg?: string;
  color: string;
  card?: boolean;
  stories?: boolean;
  bar?: boolean;
  cta?: boolean;
}

export type { HeroTextContent };
